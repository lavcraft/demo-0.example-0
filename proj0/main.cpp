#include <iostream>
#include "dep0/dep0.h"
int main(int argc, char** argv)
{
    std::cout << sum(1,2);
    std::cout << sum(1,3);
    std::cout << sum(1,3);
    std::cout << std::endl;
    return 0;
}
